/*
 * BattlePage Messages
 *
 * This contains all the text for the BattlePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BattlePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BattlePage container!',
  },
});
