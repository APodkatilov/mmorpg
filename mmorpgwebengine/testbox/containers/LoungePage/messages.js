/*
 * LoungePage Messages
 *
 * This contains all the text for the LoungePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LoungePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoungePage container!',
  },
});
