const BattleEvents = Object.freeze({
  BattleCreate: 'BattleCreate',
  BattleConnect: 'BattleConnect',
  BattleStart: 'BattleStart',
});

const PlayerEvents = Object.freeze({
  PlayerConnect: 'PlayerConnect',
  PlayerLeave: 'PlayerLeave',
});

export { BattleEvents, PlayerEvents };
